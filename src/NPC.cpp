//For questions on this class, see Mason
#include <string>;
//#include <Handler/Clue.cpp>;
#include <list>;


class NPC {

public: 
	
	//std::string npcName = "placeholder name";
	//std::string profession = "placeholder profession";
	//int age;
	//std::list<Clue> relatedClues; //not sure how we're going to relate clues to NPCs yet, but this is here for now.
	//We mentioned doing some random backstories, but i think that the backstory generation might be its own thing entirely.

	//there's obviously going to be more methods here eventually for getting data from the class, but we don't know what's going to be needed yet.
};